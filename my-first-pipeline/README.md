# A simple jenkins pipeline to verify if the docker slave configuration is working as expected.
This project demonstrates a single-stage Jenkins pipeline that uses a Docker container as a Jenkins agent to run the pipeline in an isolated and consistent environment.

